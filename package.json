{
  "name": "trustlens-ai",
  "private": true,
  "version": "1.0.0",
  "description": "TrustLens AI - React Frontend & Deepfake Detection Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
