# Copy this file to .env.local for local development (optional)
# Set this in Vercel's Environment Variables dashboard for production

# The URL of your deployed backend (e.g., on Railway, Render, etc.)
# Leave empty to use same-origin (only works if backend is on same domain)
VITE_API_URL=https://your-backend-url.com
